[build]
buildCommand = "cd miniapp && npm ci && npm run build && cd .. && uv sync --frozen --no-dev --extra api"

[deploy]
startCommand = "uv run python run_http.py --host 0.0.0.0 --port ${PORT:-8000}"
